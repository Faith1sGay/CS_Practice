using System;

namespace IdkImMessingAround
{
    internal class Program
    {
        private static void Main()
        {
            Console.WriteLine(Owo("a"));
            Environment.Exit(Dispose(0));
        }

        internal static string Owo(string o)
        {
            return o;
        }

        internal static int Dispose(int x)
        {
            return x;
        }
    }
}